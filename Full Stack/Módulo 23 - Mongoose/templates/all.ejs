<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
    <title>Links</title>
</head>
<body>
    <div class="container">
        <h1>Links</h1>
        <a href="/add">Adicionar link</a>
        <% for(let i = 0; i < docs.length; i++){ %>
            <div class="card p-3 mb-3" id="<%= docs[i]._id %>">
                <form class="d-flex align-items-center justify-content-between" action="/?_method=DELETE" method="POST">
                    <div class="d-flex flex-column">
                        <h2><%= docs[i].title %></h2>
                        <div><%= docs[i].description %></div>
                        <div class="text-muted"><%= docs[i].url %></div>
                        <div class="text-info"> <i class="material-icons">bar_chart</i> Clicks: <%= docs[i].click %></div>

                    </div>
                    <div class="d-flex flex-column">
                        <input name="id" value="<%= docs[i]._id %>" hidden>
                        <a class="btn btn-info mb-1" href="/edit/<%= docs[i]._id %>">Edit</a>
                        <button class="btn btn-danger" type="submit">Delete</button>
                    </div>
                </form>
            </div>
        <% } %> 
    </div>
</body>
</html>