document.body.innerHTML = '<h1>Hello World</h1>' + document.body.innerHTML; // Adiciona um elemento HTML no corpo do documento.

/* PROPRIEDADES E METODOS

activeElement: Elemento que está sendo editado.

addEventListener(): Adiciona um evento a um elemento.

adoptNode(): Adota um elemento de outro documento.

anchors: Retorna uma lista de todos os elementos <a> do documento.

applets: Retorna uma lista de todos os elementos <applet> do documento.

baseURI: Retorna o URI base do documento.

body: Retorna o elemento <body> do documento.

close(): Fecha o documento.

cookie: Retorna ou define o cookie do documento.

charset: Retorna ou define o charset do documento.

characterSet: Retorna ou define o charset do documento.

createAttribute(): Cria um atributo.

createComment(): Cria um comentário.

createDocumentFragment(): Cria um fragmento de documento.

createElement(): Cria um elemento.

createEvent(): Cria um evento.

createTextNode(): Cria um nó de texto.

defaultView: Retorna a janela de visualização padrão.

designMode: Retorna ou define o modo de design do documento.

doctype: Retorna o tipo de documento.

documentElement: Retorna o elemento raiz do documento.

documentMode: Retorna o modo de documento.

documentURI: Retorna o URI do documento.

domain: Retorna o domínio do documento.

domConfig: Retorna a configuração do DOM.

embeds: Retorna uma lista de todos os elementos <embed> do documento.

execCommand(): Executa um comando.

forms: Retorna uma lista de todos os elementos <form> do documento.

fullscreenElement: Retorna o elemento que está em fullscreen.

fullscreenEnabled(): Retorna se o fullscreen está habilitado.

getElementById(): Retorna um elemento pelo seu ID.

getElementsByClassName(): Retorna uma lista de elementos pelo seu nome de classe.

getElementsByName(): Retorna uma lista de elementos pelo seu nome.

getElementsByTagName(): Retorna uma lista de elementos pelo seu nome de tag.

hasFocus(): Retorna se o documento está em foco.

head: Retorna o elemento <head> do documento.

images: Retorna uma lista de todos os elementos <img> do documento.

implementation: Retorna a implementação do documento.

importNode(): Importa um elemento de outro documento.

inputEncoding: Retorna o encoding de entrada do documento.

lastModified: Retorna a data da última modificação do documento.

links: Retorna uma lista de todos os elementos <link> do documento.

normalize(): Normaliza o documento.

normalizeDocument(): Normaliza o documento.

open(): Abre o documento.

querySelector(): Retorna um elemento pelo seu seletor CSS.

querySelectorAll(): Retorna uma lista de elementos pelo seu seletor CSS.

readyState: Retorna o estado do documento.

referrer: Retorna o referrer do documento.

removeEventListener(): Remove um evento de um elemento.

renameNode(): Renomeia um elemento.

scripts: Retorna uma lista de todos os elementos <script> do documento.

strictErrorChecking: Retorna se o documento está em modo de erro estrito.

title: Retorna o título do documento.

URL: Retorna a URL do documento.

write(): Escreve um texto no documento.

writeln(): Escreve um texto no documento.

*/