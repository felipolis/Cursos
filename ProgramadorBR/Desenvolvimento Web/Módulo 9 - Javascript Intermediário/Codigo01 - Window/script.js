window.console.log("Primeiro Olá mundo!");
console.log("Segundo Olá mundo!");

window.alert("Primeiro Olá mundo!");
alert("Segundo Olá mundo!");

/*
PROPRIEDADES:

closed: Indica se a janela está aberta ou fechada.

console: Objeto que representa a janela de console do navegador.

defaultStatus: Texto que aparece no status bar.

document: Objeto que representa o documento HTML da janela.

frameElement: Elemento que representa o frame da janela.

frames: Objeto que representa os frames da janela.

history: Objeto que representa o histórico da janela.

innerHeight: Altura da janela, em pixels, sem o cabeçalho e o rodapé.

innerWidth: Largura da janela, em pixels, sem o cabeçalho e o rodapé.

length: Quantidade de frames da janela.

localStorage: Objeto que representa o armazenamento local da janela.

location: Objeto que representa a localização da janela.

name: Nome da janela.

navigator: Objeto que representa o navegador da janela.

opener: Objeto que representa a janela pai.

outerHeight: Altura da janela, em pixels, incluindo o cabeçalho e o rodapé.

outerWidth: Largura da janela, em pixels, incluindo o cabeçalho e o rodapé.

pageXOffset: Posição horizontal do scroll da janela.

pageYOffset: Posição vertical do scroll da janela.

parent: Objeto que representa a janela pai.

screen: Objeto que representa a tela da janela.

screenLeft: Posição horizontal da janela na tela.

screenTop: Posição vertical da janela na tela.

screenX: Posição horizontal da janela na tela.

screenY: Posição vertical da janela na tela.

sessionStorage: Objeto que representa o armazenamento de sessão da janela.

scrollX: Posição horizontal do scroll da janela.

scrollY: Posição vertical do scroll da janela.

self: Objeto que representa a janela atual.


METODOS:

alert(): Exibe uma caixa de diálogo com um único botão de OK.

atob(): Converte um valor base64 para um valor binário.

blur(): Remove o foco da janela atual.

btoa(): Converte um valor binário para um valor base64.

clearInterval(): Cancela uma chamada de função repetitiva.

clearTimeout(): Cancela uma chamada de função de tempo.

close(): Fecha a janela atual.

confirm(): Exibe uma caixa de diálogo com um único botão de OK e Cancelar.

focus(): Coloca o foco na janela atual.

getComputedStyle(): Retorna a propriedade de estilo da janela atual.

getSelection(): Retorna a seleção atual.

matchMedia(): Retorna um objeto MediaQueryList.

moveBy(): Move a janela atual para um novo local.

moveTo(): Move a janela atual para um novo local.

open(): Abre uma nova janela.

print(): Imprime a página atual.

prompt(): Exibe uma caixa de diálogo com um único botão de OK e Cancelar.

requestAnimationFrame(): Retorna uma chamada de função de animação.

resizeBy(): Redimensiona a janela atual.

resizeTo(): Redimensiona a janela atual.

scroll(): Move o scroll da janela atual.

scrollBy(): Move o scroll da janela atual.

scrollTo(): Move o scroll da janela atual.

setInterval(): Retorna uma chamada de função repetitiva.

setTimeout(): Retorna uma chamada de função de tempo.

stop(): Para a execução da janela atual.

*/